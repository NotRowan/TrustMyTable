import Screen from '../../../layout/Screen';
import { Text, View } from 'react-native';

const UserProfileScreen = ({}) => {
	// Initialisation ---------------
	// State ------------------------
	// Handlers ---------------------
	// View -------------------------
	return (
		<Screen>
			<View>
				<Text>Manage Profile Screen</Text>
			</View>
		</Screen>
	);
};
export default UserProfileScreen;
