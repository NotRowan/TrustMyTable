import Screen from '../../../layout/Screen';
import { Text, View } from 'react-native';

const UserReviewsScreen = ({}) => {
	// Initialisation ---------------
	// State ------------------------
	// Handlers ---------------------
	// View -------------------------
	return (
		<Screen>
			<View>
				<Text>User Reviews Screen</Text>
			</View>
		</Screen>
	);
};
export default UserReviewsScreen;
